使用Go语言的原生标准库（不依赖第三方包），实现一个简单的定时任务调度系统。该系统应该包含以下核心功能：

1. 支持周期性任务调度（如每天、每周的固定时间触发任务）
   - 实现基于cron表达式的任务调度机制
   - 支持秒、分钟、小时、日、月、星期等字段的解析
   - 提供预定义的时间模式（如每天特定时间、每周特定时间等）

2. 生成测试文件
   - 创建完整的单元测试覆盖所有主要功能
   - 包含边界条件测试和错误处理测试
   - 验证任务调度的准确性和可靠性

3. 生成文档
   - 编写详细的API文档说明
   - 提供使用示例和最佳实践指南
   - 包含配置说明和常见问题解答

请参考项目中的现有代码结构，在 ​scron​ 相关目录下创建新的定时任务模块，并确保代码遵循Go语言的最佳实践，包括：
- 适当的注释（函数、类型、变量的详细说明）
- 完善的错误处理机制
- 符合Go惯例的代码风格
- 完整的测试用例（使用testing包）
- 遵循Go标准库的设计模式

实现应基于 time 包、context 包和其他Go标准库组件来构建，不得引入任何第三方依赖。代码需要具备良好的性能、可靠性和可扩展性。